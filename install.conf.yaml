- defaults:
    link:
      create: true
      force: true
      relink: true
       

- clean: ['~']

- link:
    ~/.gitconfig:
      path: git/gitconfig

- shell:
  - [git submodule update --init --recursive, Installing submodules]

